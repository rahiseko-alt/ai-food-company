# AGENTS.md

このファイルはリポジトリ全体の **デフォルトルール** です。すべての AI コーディングツール
（Claude Code / Codex / Cursor / Antigravity ほか）がこのファイルを参照します。

このリポジトリは `https://food.kouheikosehira.com/` で公開する静的サイト専用です。
Next.js アプリや再利用テンプレートではありません。

## このファイルの権威

- グローバル設定や個人の慣習とこのファイルが矛盾する場合、**常にこのファイルを優先** する。
- より深いディレクトリに `AGENTS.md` がある場合、そのディレクトリ配下では **近い方が優先**。
  書いていない項目だけ、このルートを継承する。

## 正とする場所

- 公開物は `site/` だけです。Cloudflare Pages の出力先も `site` です。
- 作業前の受入条件と進捗の正は `docs/roadmap.html` です。
- 公開手順は `docs/site/DEPLOY.md`、日常の変更手順は
  `docs/site/MAINTENANCE.md` を参照します。
- 過去の失敗は `docs/failures.md` に追記し、既存記録を消しません。

## 変更してよい場所

- 画面、文言、画像、演出、フォーム: `site/`
- 品質検査と補助スクリプト: `scripts/`、`tests/`、ルートの設定ファイル
- 運用資料: `docs/site/`
- CI: `.github/workflows/`

未確定の料金、対応地域、返信時間、補助金、連絡方法などを推測で確定しないでください。

## 軽微な変更は儀式なしで直接やる（2026-08-01 マスター指示）

このリポジトリは**HP専用の静的サイト**であり、複雑なアプリではない。**一文・一単語の変更を軽く速く
できることを優先する。** 以下に該当する変更は、下の「案件の絶対起点」節（原子ツリー・ゴール定義ゲート）
も「セッション開始の儀式」の `meta.active`/`meta.next` 報告も不要。実装 → 下記「必須チェック」の
常時項目を確認 → コミット、で終えてよい（PRとCIそのものは省略しない。省略するのは事前の分解・
ゲートの儀式）。

- **軽微＝既存の枠組みの中で完結する変更**：既存ページ内の文言・数値・日付・料金・画像の差し替え、
  既存要素の色・余白・フォントサイズ等の調整、誤字脱字やリンク切れの修正。
- **軽微に当たらない＝下の「案件の絶対起点」が必要**：新しいセクション・機能・ページ・ファイルの追加、
  既存の構成・導線・UI操作方式を作り直す変更（例：クリック式アコーディオンを静的な一覧に変える等の
  構造変更）、対象言語・対象地域などサイトの前提スコープ自体を変える変更、複数ファイルにまたがる
  大きな構造変更。
- 迷ったら「案件の絶対起点」側（重い方）を選ぶ。**この軽微区分自体を、都合よく変更を軽微側に
  寄せて判断しない**（原子性の判定と同じく、対象を実際より小さく見せて手順を飛ばすのが最も起きやすい
  すり抜けであるため）。

## 案件の絶対起点：まず原子ツリー（新規・構造変更のみ。軽微な変更は上記の通り対象外）

**軽微な変更に当たらない案件を始めるとき、最初に必ず作る成果物は `docs/roadmap.html` の原子分解ツリーである。**
（対象の絞り込みそのものは上記の通りだが、対象に当たった以上はAIに「今回は省く／後で作る／md で代替する」
等の判断の余地は無い。）

- **ツリーを描く直前に「ゴール定義ゲート」を通す（正＝`docs/goal-gate.md`）**：ツリーは「**渡されたゴールを
  正しく割れているか**」しか見ない。未熟なゴールを渡せば未熟なまま律儀に割られ、ずれは実装後に手戻りとして出る。
  そこで**ゴールが割るに足るか**を先に判定する。三段構成で**本人採点にしない**——サブA(`goal-drafter`)が8問に
  答える → サブB(`basis-reviewer`)が却下条件を1個ずつ当てる → **人間が争点ごとに「直させる／通す（理由を記録）」を
  選ぶ**。ゲートは通行止めではなく書き直させる装置で、通すかどうかの最終判断は常に人間が持つ。回答は
  `docs/roadmap.html` の `meta.goalgate` に残す（別ファイルに置くと未マージのまま消える）。
  ＝**第一成果物が `docs/roadmap.html` であることは変わらない**（ゲートの回答もツリーも同じファイルに載る）。
  **A と B は必ず別のサブエージェントにする**（同一セッションが書いて採点する＝本人採点は禁止）。
- **対象＝上記「軽微な変更」に当たらないあらゆる依頼**：画面や文言の変更だけでなく、既存システムの
  セキュリティ改善、調査・資料作成 等、**軽微でなければ種類・粒度を問わず同じ**。
  ユーザーの指示を受けたら、着手の第一手は常に「ゴールを頂点にした原子レベルの分解ツリーを描くこと」。
- **ツリーが出来るまで実装に入らない（強制の順序）**：ツリー（ゴール＋原子まで割った状態群＋各葉の
  `criteria`/`verify`）を描き切る前は、**いかなる実装・調査・ドキュメント生成・ファイル作成にも着手しない**。
  ヒアリングや前提確認は「ツリーを描くための入力」であって、作業ノードの実行はツリー確定後に限る。
- **形式は `docs/roadmap.html` の JSON のみ（正）**：Markdown のチェックリストや散文のロードマップで
  代替することを**禁止**する。正は `<script id="roadmap-data">` の JSON。機械（`roadmap-required` /
  `verify-roadmap-evidence.mjs`）が形式・更新・evidence を審判する。
- **原子性・書式の正**：どこまで割るか（1葉＝1事実＝1verify）と `criteria`/`verify`/`evidence` の書式は、
  下の「検証の規律」節と `docs/roadmap.html` の README シートが正。ここでは「必ず最初に作る」ことだけを固定する。

## 普遍ルール（言語・クラウドに依存しない）

どの案件・どの言語でも常に守る、土台のルール。

- **ユーザーへの態度・口調は常に丁寧に保つ（維持厳守）**。敬意ある丁寧語（です・ます調）で応対し、
  乱暴・ぞんざいな言葉遣い（「お前」等のぞんざいな呼称、高圧的・命令口調、見下し表現）は禁止。
  技術的に率直・簡潔であることと、丁寧であることは両立させる（率直さを口調の乱暴さの言い訳にしない）。
- 秘密情報（API キー等）はコミットしない。`.env*` は Git 管理外。
- 変更後は、下記「必須チェック」を通してからコミットする。
- 既存のコードスタイル・構成に合わせる。**指示のない大規模リファクタリングは禁止**。
- 依存ライブラリの追加やパッケージマネージャーの混在は勝手にやらない（理由を添えて提案する）。
- コミットは小さく、説明的に。

## 結合を増やさない

- 入口は `site/assets/js/main.mjs` の1つにします。機能は独立した ES Module に分けます。
- オープニング、Lottie読込、スクロール演出、メニュー詳細、フォーム送信の状態を
  互いのDOM移動量やタイマーに依存させません。
- 本文、お品書き、フォームをオープニングやスクロール演出の `transform` 対象にしません。
- CSSで通常フローを成立させ、JavaScriptが失敗しても本文と問い合わせ導線を表示します。
- 同じ文言やメニュー情報を複数箇所へ手書きしません。
- 例外的な数値、セレクター、URLは各機能の設定へ集約し、別機能から上書きしません。

## 検証の規律（恒久ルール）— 本人採点の禁止

作業した本人が「できました」と言っても、それは証拠にならない。done 判定は次の 2 階建てで行う。

- **二層構造**：`言語化`（何/なぜ＝状態・`criteria`・`verify`。着手前に固定し、git で不変に扱う）→
  `手順化`（どう＝作業。都度・使い捨て）。境目は `verify`。ゲートは「手順化→言語化」への一方通行
  （evidence を verify が食って初めて状態が満たされる）。
- **検証は本人に採点させない＝2階建て**：
  1. 機械で白黒つく所（型/Lint/テスト/ビルド/URL到達）は **機械（CI＝`.github/workflows`）** が判定する。
  2. 機械で白黒つかない所（画面が正しい・主要フローが成功する 等）だけ、**独立サブエージェント**
     （`.claude/agents/independent-verifier.md`）が判定する。＝作業した本人以外・報告を鵜呑みにせず
     自分で `verify` を再実行・再観察し、敵対的に落としにいく。
- **機械強制（サーバー側設定）を切らない**：既定ブランチ `main` の branch protection / Ruleset で
  「`ci-green` が緑でないとマージ不可」「PR 必須（Required approvals は `0` のまま）」を有効に保つ。
  クローンで運ばれない唯一のサーバー側設定で、外すとルール文から歯が抜ける。
  ⚠ GitHub Actions の自動トークン（`GITHUB_TOKEN`）には branch protection を変える権限が無く、
  workflow からの自動化は不可（`docs/failures.md` 参照）。必ず人の管理者権限で行う。
- **基準は着手前に固定**：`criteria` / `verify` は着手前に決め、作業の途中で自分に都合よく緩めない。
- **evidence は偽造不能な外部事実のみ**：CI の run URL / 変更を実際に含む commit SHA / デプロイ ID /
  第三者が叩ける公開 URL。**「スクショ保存した」「レビューした」等の自己申告は証拠にしない。**
- **粒度＝原子（1葉＝1事実＝1verify）。これが唯一の停止条件**：葉の状態は「独立して落ちうる受入事実」が
  **ちょうど1つ**になるまで割る。**原子まで割り切ること以外に葉を確定してよい条件は無い**。AI・セッションで揺らがせない。
  - **判定は決定的（この1問だけで機械的に決まる。主観で止めない）**：
    **その状態の `verify` は「独立して落ちうる"受入事実"」を2つ以上含むか？**
    **YES → まだ葉ではない＝必ず割る。NO（受入事実がちょうど1つ）→ そこで初めて葉として確定する。**
    「概念的に1つ」「だいたい1機能」「1つの責任単位っぽい」等の主観判断で止めることは**禁止**。
  - **数えるのは"受入事実"の数であって、`&&` やコマンドの数ではない**：ここで言う"受入事実"とは
    **マスターが独立に受け入れ判定したい結果／能力**を指す（内部の失敗原因ではない）。
    - 例A＝**1事実**：`lint && test`（複合コマンド）は「品質が機械で緑」という**1つの受入結果**を
      検証しているだけ。lint と test は同じ結果の構成手段であって独立した受入事実ではない → NO → 1葉のまま。
    - 例B＝**2事実**：「検知(secret scanning)が有効」と「阻止(push protection)が有効」は、マスターが**別々に**
      受け入れたい2つの能力 → YES → 2葉に割る。
    - 迷ったら「この結果が欠けても、もう一方は独立して受け入れ可能か？」を問う。可能なら別事実＝割る。
  - **「割りすぎの歯止め」は原子性を緩めない従属条項**：歯止めが禁じるのは、**既に原子化した1事実を検証手段の
    都合でさらに機械的に刻む**こと（例：1コマンドを引数ごと・アサーションごとに別葉へ分ける）**だけ**。
    **独立して落ちうる事実が2つ以上残っている限り、「責任単位」を口実に分解を止めてはならない**（原子化が主、細分の抑止が従）。
  - その下（操作＝作業ノード）は着手時に展開する（遅延展開）。
- **平易言語（人間が監査できる形）**：各状態の `theme` は**非エンジニアが読んで「何が嬉しいか」が分かる1行**にする。
  専門用語（12-Factor / OWASP 等）は根拠として `detail` へ退避。これが無いと人間（マスター）の承認が形骸化する。
- **承認はシンプル（tier / basis-gate 方式は廃止）**：変更は PR を出せば **誰でもレビュー/承認してマージ**できる。
  承認の階層（tier-0/1/2）や「この変更は許可が要るか」を判定する検問所（basis-gate）は用いない。
  守るのは **CI が緑であること** だけ。

## 必須チェック

Node.js 22 と pnpm 10.33.0 を使用します。ビルド処理はありません。

検査は「常時・自動（数秒〜数十秒）」と「該当時のみ（数分）」に分けている。CI
（`.github/workflows/ci.yml`）の `ci-green` が必須にしているのは前者だけで、後者はマージの必須条件に
していない（2026-08-01 マスター指示：一文・一単語の変更を軽く速くできることを優先する）。

### 常時・自動（push/PRのたびにCIが自動実行。コミット前にローカルで待つ必要はない）

```sh
pnpm install --frozen-lockfile
pnpm run verify:references
pnpm run verify:lottie
pnpm run verify:asset-version
pnpm run verify:i18n
pnpm run lint
pnpm run test:unit
pnpm run audit
pnpm run verify:roadmap
```

いずれも数秒〜数十秒で終わる。CIが自動で実行するため、コミット前にローカルで走らせる義務はない
（心配なら `pnpm run lint && pnpm run test:unit` 程度の軽いものだけ手元で確認すれば足りる）。

### 該当時のみ（JS・演出・操作方式に触れる変更のときだけ）

```sh
pnpm run test:e2e   # 約5分
pnpm run test:a11y  # 約1.5分
```

`site/assets/js/`・`site/index.html`・`tests/e2e/`・演出関連ファイル（`playwright.config.mjs`、
`site/assets/vendor/`）を変更したときだけ、CIの `browser` ジョブが自動で走る（`.github/workflows/ci.yml`
の `changes` ジョブが対象ファイルの変更を判定する）。文言・数値・色・余白だけの変更ではこのジョブ自体が
起動しない。念のため手元で確認したい場合のみ実行する。

検査を省略した場合や実行できなかった場合は、完了報告に理由を明記します。テストを通すために
検査を無効化したり、空のテストや `echo` だけのスクリプトを置いたりしません。

- テストも lint も**本物**だけを置く。`echo` による見かけの成功は偽の緑として扱い、禁止。
- **状態フラグではなく実物を検査する**：「正しい方を選んだか」だけを見る検査は、
  「選んだが画面には何も出ていない」を見逃す。画面に関する検査は描画結果そのものを測る。

## PR instructions（コミット/PR）

- コミットは小さく・説明的に。1 コミット＝1 目的（ロードマップの作業ノード 1 個に対応）。
- 上記「必須チェック」を緑にしてからコミットする。
- 変更は PR を出し、**誰でもレビュー/承認してマージ**してよい（tier / basis-gate の承認階層は廃止）。守るのは CI が緑なことだけ。
- ロードマップの `evidence` には外部事実（commit SHA / CI run URL 等）だけを書く。
- **全PRは `docs/roadmap.html` を必ず更新する**（例外なし。②今回トラブル・③`meta.next` は毎セッション書けるので diff 0 はあり得ない）。CI 関所 `roadmap-required` が差分0のPRを弾く。

## Git と公開

- ユーザーの未コミット変更を消したり、無関係なファイルを整形したりしません。
- 公開、push、PR作成は明示されたときだけ行います。
- Cloudflare Pages の実プロジェクト名は `ai-food-company` です。GitHub リポジトリ名は
  `rahiseko-alt/kose-food-ai-hp` であり、両者を混同しません。
- apex、`www`、Google Workspace のMXレコードは変更しません。

## セッション開始の儀式（ロードマップ）

- ロードマップの**正は `docs/roadmap.html` 内の JSON**（`<script id="roadmap-data">`）。日々の更新は**その JSON だけ**を編集する。判定・受入ゲートは末尾の描画エンジンが担い、**モデルを変える時だけ**エンジンを触る。
- **セッション開始時**：まず `docs/roadmap.html` の `meta.active`（現在地）と `meta.next`（次の一手）を読み、ユーザーに**現在地と次の一手を報告してから**作業に入る。
- **引継ぎ（handoff）**：ロードマップに触れるセッションでは `checkin-checkout` スキルに従う。handoff は独立ファイルではなく **`docs/roadmap.html` の `meta.handoff`** に一体化する（**①今回実施=`meta.handoff.done` / ②今回トラブル=`meta.handoff.trouble`（無ければ「無し」）/ ③次回やる事=`meta.next`（無ければ「ロードマップの続き」）**）。開始時に読み、ロードマップJSONを更新する瞬間に上書きして commit & push する。handoff には進捗・criteria/verify/evidence を書かない（二重管理禁止）。
  - **なぜ本編同梱か**：handoff を本編（毎PRで必ず main に乗る `docs/roadmap.html`）と同じファイルに置くことで、「別枝に書いて未マージで消える」事故を構造的に防ぐ。旧 `docs/handoff.md` は廃止。
  - **失敗は蓄積、handoff は上書き**：ミス・失敗は上書きだと消えるので `docs/failures.md` に **append（消さない）** で積む（日付＋事象＋根因＋教訓）。`meta.handoff.trouble` は今回セッションの揮発メモに留める。

### ロードマップの仕組みの正は roadmap.html 側（AGENTS.md では繰り返さない）

ロードマップは**機械制御と一体の自己完結システム**で、仕組みの説明を AGENTS.md に再掲するのは二重管理になる。

- **仕組み・仕様の正は `docs/roadmap.html` の README シート**：ノードの種別（状態/作業）、完了ゲート、分解規則、割り方、カード表記、受入条件（`criteria`/`verify`/`evidence`）の書き方はすべてそこを見る。AGENTS.md では繰り返さない。
- **機械が自動で強制する**：描画エンジンが完了ゲート（子が全 `done` でも受入未検証なら「検証待ち」）を自動導出し、`scripts/verify-roadmap-evidence.mjs` が `evidence` を外部事実か機械検査、`roadmap-required` が更新必須を強制する。仕組みは roadmap と一体で自己完結しているので、AGENTS.md 側の再説明は要らない。
- **criteria/verify を着手前に固定し AI が書き換えない**規律は、上記「検証の規律」節が正（門＝人間承認）。
